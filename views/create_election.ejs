<!DOCTYPE html>
<html lang="en">
<head>
    <title>New Election | Online Voting Platform</title>
    <meta name="csrf-token" content="<%= csrf %>" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css" integrity="sha512-MV7K8+y+gLIBoVD59lQIYicR65iaqukzvf/nwasF0nqhPay5w/9lJmVM2hMDcnK1OnMGCdVK+iQrJ7lzPJQd1w==" crossorigin="anonymous" referrerpolicy="no-referrer"/>
    <style>
      .btn-success:hover{
        background-color: grey;
      }
      body {
  height: 100vh;
  /* background-color: red;  */
  background-image: linear-gradient(0deg, cyan, pink);
}
    </style>
</head>
<body >
  <!-- <img src="bg5.jpeg" style="opacity: 0.8;position: absolute;top: 0px; height: 100%;width: 100%;z-index: -1;" alt=""> -->
  <div >
    <%- include ('header', {username: username}) %>
  </div>
  <div >
   
    <div >
      <% if (messages && messages.error && messages.error.length > 0) { %>
        <% for(var i=0; i<messages.error.length; i++) {%>
          <div > <%= messages.error[i] %></div>
        <% } %>
      <% } %>
    </div>
    <div class="text-center">
     
      <div >
        <p class="h2 mt-5">Enter a New Election</p>
        <div >
          <form action="/election" method="post">
            <input type="hidden" name="_csrf" value="<%= csrf%>">
            <div class="mt-5 ">
              <label class="h3  " for="name">Name</label>
              <input class="form-control w-25  m-auto" type="text" name="name" placeholder="Election Name"/>
            </div>
            <button class="mt-3 btn btn-success" type="submit" >
              Submit
            </button>
          </form>
        </div>
      </div>
    </div>    
  </div>
</body>
</html>
<html>
  
